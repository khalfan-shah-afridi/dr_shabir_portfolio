import React,{useState} from "react";
import {useNavigate} from "react-router-dom";
import {Mail,ShieldCheck,LockKeyhole} from "lucide-react";
import api from "../../services/api";
import "./Login.css";
export default function ForgotPassword(){const nav=useNavigate();const [step,setStep]=useState(1),[email,setEmail]=useState(""),[otp,setOtp]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[msg,setMsg]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const run=async fn=>{try{setBusy(true);setError("");await fn()}catch(e){setError(e.response?.data?.message||"Request failed")}finally{setBusy(false)}};return <div className="login-page"><div className="login-card"><div className="login-title"><ShieldCheck size={28}/><div><span>SECURE ACCOUNT RECOVERY</span><h1>Forgot Password</h1></div></div>{msg&&<div className="login-success">{msg}</div>}{error&&<div className="login-error">{error}</div>}{step===1&&<form onSubmit={e=>{e.preventDefault();run(async()=>{await api.post('/auth/forgot-password/send-otp',{email});setMsg('OTP sent to your Gmail if the account exists.');setStep(2)})}}><label>Email<><Mail size={15}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></></label><button disabled={busy}>{busy?'Sending...':'Send Gmail OTP'}</button></form>}{step===2&&<form onSubmit={e=>{e.preventDefault();run(async()=>{await api.post('/auth/forgot-password/verify-otp',{email,otp});setMsg('OTP verified. Create a new password.');setStep(3)})}}><label>6-digit OTP<input value={otp} onChange={e=>setOtp(e.target.value)} maxLength={6} required/></label><button disabled={busy}>{busy?'Verifying...':'Verify OTP'}</button></form>}{step===3&&<form onSubmit={e=>{e.preventDefault();if(password!==confirm)return setError('Passwords do not match.');run(async()=>{await api.post('/auth/forgot-password/reset',{email,password});setMsg('Password reset successfully. Redirecting to login...');setTimeout(()=>nav('/login'),1200)})}}><label>New Password<><LockKeyhole size={15}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/></></label><label>Confirm Password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} minLength={6} required/></label><button disabled={busy}>{busy?'Updating...':'Reset Password'}</button></form>}<button className="login-secondary" onClick={()=>nav('/login')}>Back to Login</button></div></div>}
